Écrire un programme qui imprime la moyenne arithmétique (a+b)/2 de deux nombres de type float lus sur input.
Votre code à tester par UpyLaB ne doit pas avoir de paramètre dans les input. Exemple: a = float(input()) plutôt que a = float(input("a = "))
Dans la colonne Résultats, le caractère "?" représente le caractère "passage à la ligne" appelé également "retour chariot" ("return" ou "carriage return" en anglais).